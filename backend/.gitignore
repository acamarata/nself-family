# Local private planning context
.ai/

# Local env files (ignore local overrides and secrets)
.env
.env.local
.env.secrets

# But DO commit environment templates
# (These are checked in: .env.dev, .env.staging, .env.prod)

# Docker volumes (local data)
.volumes/

# Build outputs
build/
dist/
out/
coverage/

# Logs
*.log
logs/
